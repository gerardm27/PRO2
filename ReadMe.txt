

Link de la pràctica: https://www.cs.upc.edu/pro2/index.php?id=practica-primavera-2020

Ajuda per a documentar la practica (a mi manera): 

            Coses importants a tenir en compte:

              - Variables i derivats de variables en cursiva (\a);
              - Certes coses a resaltar en negreta (\b);
              - Per a fer una frase cursiva fer servir (<em>...</em>)
              - Errors que puguin sorgir en warning (\warning);
              - A sobre de cada nou archiu, especificarlo (\file i \brief);
              - Per a varibles private fer un petit comentari (\short);
              - Per a fer llistes de arguments fer servir davant de cada nova linia (\arg or \li);
              - Fumada per escriure formules en latex (\formula);
              - Per a obrir una nova linia, igual que en printf (\n);
              - Si fas algun define, marcarlo aixi (\def name);
              - Quan un parametre pot ser de més d'un tipus (p.ex.: int or double) es separa aixi (\param type1|type2 name);
              - Per a fer un comentari (\note);
